# 2026-07-15 작업 일지

## 작업 내용

### README 머지 충돌 해결
- 팀원 커밋(`e0794fb`)과 로컬 커밋이 README TODO에서 충돌 — 양쪽 병합으로 해결
- 팀원 추가 항목 반영: 게시물 이미지 복사/공유(카카오톡, 인스타DM, URL), 80 포트 교체
- 폰트는 논의 끝에 Pretendard로 확정 (Freesentation 대안은 채택 안 함)

### 브랜드 아이콘 제거
- 여러 시안(말풍선, 물음표, 무도리 트레이싱) 반복 끝에 아이콘 미사용 결정
- 헤더는 텍스트 워드마크만, 파비콘은 노란 라운드 사각형

### 배포 구조 정리 (muhaha.kr)
- 서버 상태 확인: nginx 80이 전 경로를 백엔드(:5000)로 프록시 중, 프론트는 :3000에 별도 서빙, SSL 미적용
- `src/lib/api.js`: dev는 원격 백엔드 직접 호출, **프로덕션 빌드는 same-origin(`/api`)** 으로 변경
  - nginx가 `/api`, `/uploads`를 프록시하면 도메인·프로토콜을 자동으로 따라감 → HTTPS 전환 시 재빌드 불필요
- `docs/deploy/nginx-muhaha.kr.conf` 작성: 프론트 dist 정적 서빙 + 백엔드 프록시 + `client_max_body_size 12m`(업로드 413 방지)
- README 배포 체크리스트를 muhaha.kr 기준으로 재작성 (빌드 → nginx → certbot 순서)

### 기타
- `.claude/` 로컬 설정을 gitignore에 추가 (워킹트리 정리)

## 결정/메모
- 공유 기능 방향: URL 복사(즉시 가능) + Web Share API(HTTPS 후 자동 활성화), 카카오 SDK는 JS 키 발급 후, 인스타 DM은 공식 API 없어 OS 공유 시트로 갈음
- 서버에서 nginx 설정 적용 후 3000 포트 프론트 프로세스는 종료해야 함
