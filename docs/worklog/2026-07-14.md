# 2026-07-14 작업 일지

## 커밋
- `a18ebbc` 태그/폰트/모바일 UX/게시글 수정/관리자 개편/브랜드 마크 및 README 정리
- (이후) 브랜드 마크 리디자인, 작업 일지 및 CLAUDE.md 추가

## 작업 내용

### 태그 정리 (README TODO)
- Post 모델에 `tags` 필드 추가 (쉼표 구분 문자열 — DB에 이미 있던 형태에 맞춤)
- text 인덱스에 tags 포함 — **배포 시 기존 인덱스 삭제 필요**: `db.posts.dropIndex("title_text_description_text")`
- 관리자 폼에 태그 입력 추가, 메인 검색이 제목·설명·태그를 모두 매칭

### 게시글 수정 기능
- 백엔드 `PUT /api/posts/:id` 추가 (제목/설명/태그만, 이미지는 유지)
- 관리자에서 게시글별 수정 버튼 → 기존 값이 채워진 폼으로 수정

### 관리자 화면 개편
- 목록 중심 구조로 변경: 인라인 폼 제거, 상단 "새 짤 등록" 버튼
- 등록/수정을 `PostFormModal` 공용 모달로 처리 (등록: 드롭존, 수정: 이미지 교체 불가 안내)

### UX/UI (README TODO)
- 기본 폰트를 Pretendard로 교체 (jsdelivr CDN 동적 서브셋 + Tailwind 4 `@theme`)
- 모바일: 모달 전체 화면(dvh 단위), 댓글 input 16px 고정 — iOS는 16px 미만 input 포커스 시 강제 줌이 발생하므로 "mobile 댓글 input focus" 문제의 원인이었을 가능성이 높음

### 브랜드 마크 (파비콘 + 헤더 로고)
- 여러 차례 반복 끝에 확정: **무도리 캐릭터 오마주** — 사용자가 제공한 실제 무도리 레퍼런스 이미지 기반
- 구성: 통통한 물음표 블롭(굵은 스트로크 + 중앙 채움, 구멍 없음) + 정중앙 상단 왕관 + 분리된 점, 노랑 단색 (눈은 피드백으로 제거)
- 거쳐간 시안: 말풍선+"무도" → 말풍선+점3개 → 왕관+물음표 캐릭터 → 단색 물음표 → 추상 고리 → 말풍선+물음표 → 타이틀 박스+"무도" → 블롭+눈 → 최종(눈 제거, 왕관 중앙)
- 저작권 원칙: 원본 트레이싱이 아니라 비율과 좌표를 새로 잡은 오리지널 드로잉
- `LogoBubble` → `Logo` 컴포넌트로 교체, 파비콘과 동일 도형 공유

### 문서화
- README를 프로젝트 문서로 재작성 (실행/배포 체크리스트, TODO 체크오프)
- `docs/worklog/` 작업 일지 시작, CLAUDE.md 작성

## 결정/메모
- 배포(원격 서버 git pull + npm install + 재시작) 전까지 등록/수정/삭제 API는 원격에서 미동작
- 서버에 이미 배포된 업로드 파이프라인으로 실제 게시글이 올라오기 시작함 (WebP 파일명 확인)
- 사용자 피드백 반영: 이모지 금지 유지, 아이콘은 "무도스러운" 방향 (말풍선 아님, 타이틀 박스가 정체성)
