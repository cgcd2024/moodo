# 2026-07-10 작업 일지

## 커밋
- `5f85f54` UI 전면 개선: 레이아웃 개편, 컬러 통일, 로딩/에러 상태 및 버그 수정

## 작업 내용

### UI 전면 개선
- 메인 페이지에 전체 짤 그리드 상시 노출 (기존에는 검색해야만 목록이 보였음)
- 캐러셀을 추천순 "인기 짤"로 변경 (순위 뱃지 + 좌우 화살표 버튼)
- 오늘의 무도짤을 텍스트 오버레이 히어로 카드로 재설계
- 스티키 헤더(블러) + 푸터 추가, 과했던 상하 여백 축소
- 모달의 인디고 색을 노랑 브랜드 컬러로 통일, 배경에 노랑 radial 글로우 추가
- 이미지 기본 흐림(opacity-80) 제거

### 상태 처리
- 로딩 스켈레톤, 서버 에러 화면(재시도 버튼) 추가

### 버그 수정
- MemeGrid의 `key={meme.id}` → `meme._id` (key가 전부 undefined였던 문제)
- 댓글 시간이 항상 빈 값이던 문제: 존재하지 않는 `comment.timestamp` 대신 `createdAt`을 상대시간("N분 전")으로 표시

### 인터랙션
- 투표 후 재투표 방지 (localStorage 기록 + 버튼 비활성화)
- 모달 Esc 닫기 + 열림 중 배경 스크롤 잠금

### 구조 정리
- 하드코딩된 API 주소(`http://144.24.81.60:5000`)를 `src/lib/api.js`로 일원화, `VITE_API_URL` 환경변수 지원
- 프론트 package.json에서 불필요했던 백엔드 의존성 정리 대상 확인 (mongoose, dotenv)

## 결정/메모
- develop 브랜치 생성 및 원격 푸시 시작
- 백엔드 로컬 실행은 `backend/.env`(MONGO_URI) 부재로 불가 — 원격 서버(144.24.81.60:5000) 기준으로 프론트 개발
